on:
  push:
    branches:
      - main
jobs:
  continuously-run-for-feedback:
    runs-on: ubuntu-latest
    steps:
      - name: "use the download artifact action and see what happens"
        id: hello
        uses: RequiemWorld/download-artifact
        with:
          who-to-greet: Mona the Octocat

